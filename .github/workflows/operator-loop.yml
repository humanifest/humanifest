name: Operator Loop

on:
  schedule:
    - cron: '27 13 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: operator-loop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  operate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.11'
      - name: Read-only operator loop
        run: python -m humanifest.cli operate --root . --as-of "$(date -u +%F)" --max-age-days 30
